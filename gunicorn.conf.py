# Add these 3 lines at the top
import os
from application import app  # Replace 'myapp' with your actual module name

# Then add this new config parameter:
capture_output = True  # This reveals hidden Python errors